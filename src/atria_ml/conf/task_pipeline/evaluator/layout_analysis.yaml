# @package __global__
_target_: hydra_zen.funcs.zen_processing
_zen_target: atria_ml.task_pipelines._evaluator.Evaluator
_zen_partial: false
_zen_exclude:
- pipeline_name
- n_devices
- backend
- experiment_name
defaults:
- /data_pipeline@data_pipeline: default
- /engine@test_engine: default_test_engine
- /model_pipeline@model_pipeline: object_detection
- _self_
test_checkpoint: ???
data_pipeline:
  collate_fn: mmdet_pseudo_collate
output_dir: outputs/evaluator/layout_analysis/${resolve_experiment_name:${experiment_name}}/
prepare_atria_ckpt: true
do_eval: true
pipeline_name: atria_evaluator
n_devices: 1
backend: nccl
experiment_name: _to_be_resolved_
